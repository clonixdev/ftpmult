apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: ftp-mult-tcp
spec:
  entryPoints:
    - ftp
  routes:
    - match: HostSNI(`*`)
      services:
        - name: ftp-mult
          port: 2121
